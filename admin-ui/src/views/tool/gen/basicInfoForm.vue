<template>
  <el-form ref="basicInfoForm" :model="infoForm" :rules="rules" label-width="150px">
    <el-row>
      <el-col :span="12">
        <el-form-item :label="tt('表名称')" prop="tableName">
          <el-input v-model="infoForm.tableName" :placeholder="tt('请输入仓库名称')" />
        </el-form-item>
      </el-col>
      <el-col :span="12">
        <el-form-item :label="tt('表描述')" prop="tableComment">
          <el-input v-model="infoForm.tableComment" :placeholder="tt('请输入')" />
        </el-form-item>
      </el-col>
      <el-col :span="12">
        <el-form-item :label="tt('实体类名称')" prop="className">
          <el-input v-model="infoForm.className" :placeholder="tt('请输入')" />
        </el-form-item>
      </el-col>
      <el-col :span="12">
        <el-form-item :label="tt('作者')" prop="functionAuthor">
          <el-input v-model="infoForm.functionAuthor" :placeholder="tt('请输入')" />
        </el-form-item>
      </el-col>
      <el-col :span="24">
        <el-form-item :label="tt('备注')" prop="remark">
          <el-input v-model="infoForm.remark" type="textarea" :rows="3"></el-input>
        </el-form-item>
      </el-col>
    </el-row>
  </el-form>
</template>

<script setup lang="ts">
import { propTypes } from '@/utils/propTypes';
import { tt } from '@/utils/i18nText';

const prop = defineProps({
  info: propTypes.any.def({})
});

const infoForm = computed(() => prop.info);

// 表单校验
const rules = ref({
  tableName: [{ required: true, message: tt('请输入表名称'), trigger: 'blur' }],
  tableComment: [{ required: true, message: tt('请输入表描述'), trigger: 'blur' }],
  className: [{ required: true, message: tt('请输入实体类名称'), trigger: 'blur' }],
  functionAuthor: [{ required: true, message: tt('请输入作者'), trigger: 'blur' }]
});
</script>
