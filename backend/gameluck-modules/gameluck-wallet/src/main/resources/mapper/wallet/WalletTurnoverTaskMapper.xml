<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gameluck.wallet.mapper.WalletTurnoverTaskMapper">

    <select id="selectPendingByMemberForUpdate" resultType="com.gameluck.wallet.domain.WalletTurnoverTask">
        select *
        from gl_wallet_turnover_task
        where tenant_id = #{tenantId}
          and member_id = #{memberId}
          and currency_code = #{currencyCode}
          and status = #{status}
          and del_flag = '0'
        order by create_time asc, id asc
        for update
    </select>

    <update id="cancelPendingByPurchase">
        update gl_wallet_turnover_task
        set status = #{cancelledStatus},
            remark = #{remark},
            update_time = #{now}
        where tenant_id = #{tenantId}
          and member_id = #{memberId}
          and business_no = #{businessNo}
          and status = #{pendingStatus}
          and del_flag = '0'
    </update>
</mapper>
