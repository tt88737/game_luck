<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gameluck.wallet.mapper.WalletAccountMapper">

    <select id="selectByBizKeyForUpdate" resultType="com.gameluck.wallet.domain.WalletAccount">
        select *
        from gl_wallet_account
        where tenant_id = #{tenantId}
          and member_id = #{memberId}
          and currency_code = #{currencyCode}
          and del_flag = '0'
        limit 1
        for update
    </select>
</mapper>
