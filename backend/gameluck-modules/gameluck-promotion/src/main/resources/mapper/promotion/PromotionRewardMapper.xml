<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gameluck.promotion.mapper.PromotionRewardMapper">

    <select id="selectByIdForUpdate" resultType="com.gameluck.promotion.domain.PromotionReward">
        SELECT *
        FROM gl_promotion_reward
        WHERE id = #{id}
          AND del_flag = '0'
        FOR UPDATE
    </select>

    <select id="selectActiveDailyLoginReward" resultType="com.gameluck.promotion.domain.PromotionReward">
        SELECT *
        FROM gl_promotion_reward
        WHERE tenant_id = #{tenantId}
          AND promotion_type = 'DAILY_LOGIN'
          AND status = 'ACTIVE'
          AND del_flag = '0'
          AND (start_time IS NULL OR start_time &lt;= NOW())
          AND (end_time IS NULL OR end_time &gt;= NOW())
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <select id="selectClientActiveRewards" resultType="com.gameluck.promotion.domain.PromotionReward">
        SELECT *
        FROM gl_promotion_reward
        WHERE tenant_id = #{tenantId}
          AND status = 'ACTIVE'
          AND del_flag = '0'
          AND (start_time IS NULL OR start_time &lt;= NOW())
          AND (end_time IS NULL OR end_time &gt;= NOW())
          AND (promotion_type IS NULL OR promotion_type &lt;&gt; 'DAILY_LOGIN')
        ORDER BY create_time DESC
    </select>

</mapper>
